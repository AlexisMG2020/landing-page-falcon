RewriteEngine On

# Serve existing files and directories normally.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Send all other routes to the SPA entry point.
RewriteRule ^ index.html [L]
